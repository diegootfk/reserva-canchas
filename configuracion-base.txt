

====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\api-gateway\pom.xml
====================================================

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.12</version>
        <relativePath/>
    </parent>

    <groupId>com.reservacanchas.cl</groupId>
    <artifactId>api-gateway</artifactId>
    <version>0.0.1-SNAPSHOT</version>

    <properties>
        <java.version>17</java.version>
        <spring-cloud.version>2023.0.5</spring-cloud.version>
    </properties>

    <dependencies>

        <!-- GATEWAY -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-gateway</artifactId>
        </dependency>

        <!-- SECURITY -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- JWT -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>

        <!-- SWAGGER -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webflux-ui</artifactId>
            <version>2.6.0</version>
        </dependency>

        <!-- TEST -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <dependencyManagement>
        <dependencies>

            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

        </plugins>
    </build>

</project>


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\api-gateway\src\main\java\com\reservacanchas\cl\api_gateway\ApiGatewayApplication.java
====================================================

package com.reservacanchas.cl.api_gateway;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class ApiGatewayApplication {

	public static void main(String[] args) {
		SpringApplication.run(ApiGatewayApplication.class, args);
	}

}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\api-gateway\src\main\java\com\reservacanchas\cl\api_gateway\config\SecurityConfig.java
====================================================

package com.reservacanchas.cl.api_gateway.config;

import java.nio.charset.StandardCharsets;

import javax.crypto.spec.SecretKeySpec;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import org.springframework.security.config.annotation.web.reactive.EnableWebFluxSecurity;
import org.springframework.security.config.web.server.ServerHttpSecurity;
import org.springframework.security.web.server.SecurityWebFilterChain;

import org.springframework.security.oauth2.jose.jws.MacAlgorithm;
import org.springframework.security.oauth2.jwt.NimbusReactiveJwtDecoder;
import org.springframework.security.oauth2.jwt.ReactiveJwtDecoder;

import org.springframework.security.oauth2.server.resource.authentication.ReactiveJwtAuthenticationConverter;

@Configuration
@EnableWebFluxSecurity
public class SecurityConfig {

    @Value("${spring.security.oauth2.resourceserver.jwt.secret-key}")
    private String secret;

    @Bean
    public SecurityWebFilterChain securityWebFilterChain(ServerHttpSecurity http) {

        return http
                .csrf(csrf -> csrf.disable())

                .authorizeExchange(exchanges -> exchanges

                        // AUTH
                        .pathMatchers("/auth/**").permitAll()

                        // SWAGGER Y OPENAPI
                        .pathMatchers(
                                "/swagger-ui.html",
                                "/swagger-ui/**",
                                "/webjars/**",
                                "/v3/api-docs/**",
                                "/api-docs/**"
                        ).permitAll()

                        // ADMIN
                        .pathMatchers("/usuarios/**").hasAuthority("ROLE_ADMIN")
                        .pathMatchers("/pagos/**").hasAuthority("ROLE_ADMIN")

                        .anyExchange().authenticated()
                )

                .oauth2ResourceServer(oauth2 -> oauth2
                        .jwt(jwt -> jwt.jwtAuthenticationConverter(jwtAuthenticationConverter()))
                )

                .build();
    }

    @Bean
    public ReactiveJwtDecoder jwtDecoder() {

        SecretKeySpec key = new SecretKeySpec(
                secret.getBytes(StandardCharsets.UTF_8),
                "HmacSHA384"
        );

        return NimbusReactiveJwtDecoder
                .withSecretKey(key)
                .macAlgorithm(MacAlgorithm.HS384)
                .build();
    }

    @Bean
    public ReactiveJwtAuthenticationConverter jwtAuthenticationConverter() {

        ReactiveJwtAuthenticationConverter converter =
                new ReactiveJwtAuthenticationConverter();

        converter.setJwtGrantedAuthoritiesConverter(jwt -> {

            String role = jwt.getClaimAsString("role");

            if (role == null) {
                return reactor.core.publisher.Flux.empty();
            }

            return reactor.core.publisher.Flux.just(
                    new org.springframework.security.core.authority.SimpleGrantedAuthority(
                            "ROLE_" + role
                    )
            );
        });

        return converter;
    }
}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\api-gateway\src\main\resources\application.yml
====================================================

server:
  port: 7090

spring:
  main:
    web-application-type: reactive

  application:
    name: api-gateway

  security:
    oauth2:
      resourceserver:
        jwt:
          secret-key: esta_es_nuestra_klae_secreta_123456789_viva_el_ping_pong

  cloud:
    gateway:
      routes:

        # ==========================
        # MICROSERVICIOS
        # ==========================

        - id: usuario-service
          uri: http://localhost:7091
          predicates:
            - Path=/usuarios/**

        - id: cancha-service
          uri: http://localhost:7092
          predicates:
            - Path=/canchas/**

        - id: reserva-service
          uri: http://localhost:7093
          predicates:
            - Path=/reservas/**

        - id: pago-service
          uri: http://localhost:7094
          predicates:
            - Path=/pagos/**

        - id: sede-service
          uri: http://localhost:7095
          predicates:
            - Path=/sedes/**

        - id: horario-service
          uri: http://localhost:7096
          predicates:
            - Path=/horarios/**

        - id: disponibilidad-service
          uri: http://localhost:7097
          predicates:
            - Path=/disponibilidades/**

        - id: notificacion-service
          uri: http://localhost:7098
          predicates:
            - Path=/notificaciones/**

        - id: resena-service
          uri: http://localhost:7099
          predicates:
            - Path=/resenas/**

        - id: mantenimiento-service
          uri: http://localhost:7100
          predicates:
            - Path=/mantenimientos/**

        - id: auth-service
          uri: http://localhost:7101
          predicates:
            - Path=/auth/**

        # ==========================
        # SWAGGER DOCS
        # ==========================

        - id: auth-docs
          uri: http://localhost:7101
          predicates:
            - Path=/api-docs/auth
          filters:
            - SetPath=/v3/api-docs

        - id: usuario-docs
          uri: http://localhost:7091
          predicates:
            - Path=/api-docs/usuarios
          filters:
            - SetPath=/v3/api-docs

        - id: cancha-docs
          uri: http://localhost:7092
          predicates:
            - Path=/api-docs/canchas
          filters:
            - SetPath=/v3/api-docs

        - id: reserva-docs
          uri: http://localhost:7093
          predicates:
            - Path=/api-docs/reservas
          filters:
            - SetPath=/v3/api-docs

        - id: pago-docs
          uri: http://localhost:7094
          predicates:
            - Path=/api-docs/pagos
          filters:
            - SetPath=/v3/api-docs

        - id: sede-docs
          uri: http://localhost:7095
          predicates:
            - Path=/api-docs/sedes
          filters:
            - SetPath=/v3/api-docs

        - id: horario-docs
          uri: http://localhost:7096
          predicates:
            - Path=/api-docs/horarios
          filters:
            - SetPath=/v3/api-docs

        - id: disponibilidad-docs
          uri: http://localhost:7097
          predicates:
            - Path=/api-docs/disponibilidades
          filters:
            - SetPath=/v3/api-docs

        - id: notificacion-docs
          uri: http://localhost:7098
          predicates:
            - Path=/api-docs/notificaciones
          filters:
            - SetPath=/v3/api-docs

        - id: resena-docs
          uri: http://localhost:7099
          predicates:
            - Path=/api-docs/resenas
          filters:
            - SetPath=/v3/api-docs

        - id: mantenimiento-docs
          uri: http://localhost:7100
          predicates:
            - Path=/api-docs/mantenimientos
          filters:
            - SetPath=/v3/api-docs

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    urls:

      - name: Auth Service
        url: /api-docs/auth

      - name: Usuario Service
        url: /api-docs/usuarios

      - name: Cancha Service
        url: /api-docs/canchas

      - name: Reserva Service
        url: /api-docs/reservas

      - name: Pago Service
        url: /api-docs/pagos

      - name: Sede Service
        url: /api-docs/sedes

      - name: Horario Service
        url: /api-docs/horarios

      - name: Disponibilidad Service
        url: /api-docs/disponibilidades

      - name: Notificacion Service
        url: /api-docs/notificaciones

      - name: Resena Service
        url: /api-docs/resenas

      - name: Mantenimiento Service
        url: /api-docs/mantenimientos


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\api-gateway\target\classes\application.yml
====================================================

server:
  port: 7090

spring:
  main:
    web-application-type: reactive

  application:
    name: api-gateway

  security:
    oauth2:
      resourceserver:
        jwt:
          secret-key: esta_es_nuestra_klae_secreta_123456789_viva_el_ping_pong

  cloud:
    gateway:
      routes:

        # ==========================
        # MICROSERVICIOS
        # ==========================

        - id: usuario-service
          uri: http://localhost:7091
          predicates:
            - Path=/usuarios/**

        - id: cancha-service
          uri: http://localhost:7092
          predicates:
            - Path=/canchas/**

        - id: reserva-service
          uri: http://localhost:7093
          predicates:
            - Path=/reservas/**

        - id: pago-service
          uri: http://localhost:7094
          predicates:
            - Path=/pagos/**

        - id: sede-service
          uri: http://localhost:7095
          predicates:
            - Path=/sedes/**

        - id: horario-service
          uri: http://localhost:7096
          predicates:
            - Path=/horarios/**

        - id: disponibilidad-service
          uri: http://localhost:7097
          predicates:
            - Path=/disponibilidades/**

        - id: notificacion-service
          uri: http://localhost:7098
          predicates:
            - Path=/notificaciones/**

        - id: resena-service
          uri: http://localhost:7099
          predicates:
            - Path=/resenas/**

        - id: mantenimiento-service
          uri: http://localhost:7100
          predicates:
            - Path=/mantenimientos/**

        - id: auth-service
          uri: http://localhost:7101
          predicates:
            - Path=/auth/**

        # ==========================
        # SWAGGER DOCS
        # ==========================

        - id: auth-docs
          uri: http://localhost:7101
          predicates:
            - Path=/api-docs/auth
          filters:
            - SetPath=/v3/api-docs

        - id: usuario-docs
          uri: http://localhost:7091
          predicates:
            - Path=/api-docs/usuarios
          filters:
            - SetPath=/v3/api-docs

        - id: cancha-docs
          uri: http://localhost:7092
          predicates:
            - Path=/api-docs/canchas
          filters:
            - SetPath=/v3/api-docs

        - id: reserva-docs
          uri: http://localhost:7093
          predicates:
            - Path=/api-docs/reservas
          filters:
            - SetPath=/v3/api-docs

        - id: pago-docs
          uri: http://localhost:7094
          predicates:
            - Path=/api-docs/pagos
          filters:
            - SetPath=/v3/api-docs

        - id: sede-docs
          uri: http://localhost:7095
          predicates:
            - Path=/api-docs/sedes
          filters:
            - SetPath=/v3/api-docs

        - id: horario-docs
          uri: http://localhost:7096
          predicates:
            - Path=/api-docs/horarios
          filters:
            - SetPath=/v3/api-docs

        - id: disponibilidad-docs
          uri: http://localhost:7097
          predicates:
            - Path=/api-docs/disponibilidades
          filters:
            - SetPath=/v3/api-docs

        - id: notificacion-docs
          uri: http://localhost:7098
          predicates:
            - Path=/api-docs/notificaciones
          filters:
            - SetPath=/v3/api-docs

        - id: resena-docs
          uri: http://localhost:7099
          predicates:
            - Path=/api-docs/resenas
          filters:
            - SetPath=/v3/api-docs

        - id: mantenimiento-docs
          uri: http://localhost:7100
          predicates:
            - Path=/api-docs/mantenimientos
          filters:
            - SetPath=/v3/api-docs

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    urls:

      - name: Auth Service
        url: /api-docs/auth

      - name: Usuario Service
        url: /api-docs/usuarios

      - name: Cancha Service
        url: /api-docs/canchas

      - name: Reserva Service
        url: /api-docs/reservas

      - name: Pago Service
        url: /api-docs/pagos

      - name: Sede Service
        url: /api-docs/sedes

      - name: Horario Service
        url: /api-docs/horarios

      - name: Disponibilidad Service
        url: /api-docs/disponibilidades

      - name: Notificacion Service
        url: /api-docs/notificaciones

      - name: Resena Service
        url: /api-docs/resenas

      - name: Mantenimiento Service
        url: /api-docs/mantenimientos


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\auth-service\pom.xml
====================================================

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.12</version>
        <relativePath/>
    </parent>

    <groupId>com.reservacanchas.cl</groupId>
    <artifactId>auth-service</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>auth-service</name>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>

        <!-- WEB -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- MYSQL -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- VALIDATION -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- LOMBOK -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- JWT -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.12.5</version>
        </dependency>

        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.12.5</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.12.5</version>
            <scope>runtime</scope>
        </dependency>

        <!-- SWAGGER / OPENAPI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.6.0</version>
        </dependency>

        <!-- TEST -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

        </plugins>
    </build>

</project>


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\auth-service\src\main\java\com\reservacanchas\cl\auth_service\AuthServiceApplication.java
====================================================

package com.reservacanchas.cl.auth_service;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class AuthServiceApplication {

	public static void main(String[] args) {
		SpringApplication.run(AuthServiceApplication.class, args);
	}

}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\auth-service\src\main\java\com\reservacanchas\cl\auth_service\config\SwaggerConfig.java
====================================================

package com.reservacanchas.cl.auth_service.config;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import io.swagger.v3.oas.models.servers.Server;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.List;

@Configuration
public class SwaggerConfig {

    @Bean
    public OpenAPI customOpenAPI() {

        Server gatewayServer = new Server();
        gatewayServer.setUrl("http://localhost:7090");
        gatewayServer.setDescription("API Gateway");

        return new OpenAPI()
                .servers(List.of(gatewayServer))
                .info(new Info()
                        .title("API Auth")
                        .version("1.0")
                        .description("DocumentaciÃ³n del microservicio Auth"));
    }
}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\auth-service\src\main\resources\application.yml
====================================================

server:
  port: 7101

spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_auth_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: esta_es_nuestra_klae_secreta_123456789_viva_el_ping_pong

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\auth-service\target\classes\application.yml
====================================================

server:
  port: 7101

spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_auth_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: esta_es_nuestra_klae_secreta_123456789_viva_el_ping_pong

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\cancha-service\pom.xml
====================================================

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.12</version>
        <relativePath/>
    </parent>

    <groupId>com.reservacanchas.cl</groupId>
    <artifactId>cancha-service</artifactId>
    <version>0.0.1-SNAPSHOT</version>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>

        <!-- WEB -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- JDBC -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>

        <!-- JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- LIQUIBASE -->
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>

        <!-- MYSQL -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- LOMBOK -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- VALIDATION -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- SWAGGER / OPENAPI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.6.0</version>
        </dependency>

        <!-- TEST -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

        </plugins>
    </build>

</project>


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\cancha-service\src\main\java\com\reservacanchas\cl\cancha_service\CanchaServiceApplication.java
====================================================

package com.reservacanchas.cl.cancha_service;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class CanchaServiceApplication {

	public static void main(String[] args) {
		SpringApplication.run(CanchaServiceApplication.class, args);
	}

}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\cancha-service\src\main\java\com\reservacanchas\cl\cancha_service\config\SwaggerConfig.java
====================================================

package com.reservacanchas.cl.cancha_service.config;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import io.swagger.v3.oas.models.servers.Server;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.List;

@Configuration
public class SwaggerConfig {

    @Bean
    public OpenAPI customOpenAPI() {

        Server gatewayServer = new Server();
        gatewayServer.setUrl("http://localhost:7090");
        gatewayServer.setDescription("API Gateway");

        return new OpenAPI()
                .servers(List.of(gatewayServer))
                .info(new Info()
                        .title("API Reserva Canchas")
                        .version("1.0")
                        .description("DocumentaciÃ³n del microservicio Cancha"));
    }
}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\cancha-service\src\main\resources\application.yml
====================================================

server:
  port: 7092

spring:
  application:
    name: cancha-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_cancha_service?createDatabaseIfNotExist=true
    username: root
    password:

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\cancha-service\target\classes\application.yml
====================================================

server:
  port: 7092

spring:
  application:
    name: cancha-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_cancha_service?createDatabaseIfNotExist=true
    username: root
    password:

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\disponibilidad-service\pom.xml
====================================================

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.12</version>
        <relativePath/>
    </parent>

    <groupId>com.reservacanchas.cl</groupId>
    <artifactId>disponibilidad-service</artifactId>
    <version>0.0.1-SNAPSHOT</version>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>

        <!-- WEB -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- JDBC -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>

        <!-- JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- LIQUIBASE -->
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>

        <!-- MYSQL -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- VALIDATION -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- LOMBOK -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- SWAGGER / OPENAPI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.6.0</version>
        </dependency>

        <!-- TEST -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

        </plugins>
    </build>

</project>


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\disponibilidad-service\src\main\java\com\reservacanchas\cl\disponibilidad_service\DisponibilidadServiceApplication.java
====================================================

package com.reservacanchas.cl.disponibilidad_service;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class DisponibilidadServiceApplication {

	public static void main(String[] args) {
		SpringApplication.run(DisponibilidadServiceApplication.class, args);
	}

}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\disponibilidad-service\src\main\java\com\reservacanchas\cl\disponibilidad_service\config\SwaggerConfig.java
====================================================

package com.reservacanchas.cl.disponibilidad_service.config;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import io.swagger.v3.oas.models.servers.Server;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.List;

@Configuration
public class SwaggerConfig {

    @Bean
    public OpenAPI customOpenAPI() {

        Server gatewayServer = new Server();
        gatewayServer.setUrl("http://localhost:7090");
        gatewayServer.setDescription("API Gateway");

        return new OpenAPI()
                .servers(List.of(gatewayServer))
                .info(new Info()
                        .title("API Disponibilidad")
                        .version("1.0")
                        .description("DocumentaciÃ³n del microservicio Disponibilidad"));
    }
}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\disponibilidad-service\src\main\resources\application.yml
====================================================

server:
  port: 7097

spring:
  application:
    name: disponibilidad-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_disponibilidad_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\disponibilidad-service\target\classes\application.yml
====================================================

server:
  port: 7097

spring:
  application:
    name: disponibilidad-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_disponibilidad_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\horario-service\pom.xml
====================================================

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.12</version>
        <relativePath/>
    </parent>

    <groupId>com.reservacanchas.cl</groupId>
    <artifactId>horario-service</artifactId>
    <version>0.0.1-SNAPSHOT</version>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>

        <!-- WEB -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- JDBC -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>

        <!-- JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- LIQUIBASE -->
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>

        <!-- MYSQL -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- VALIDATION -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- LOMBOK -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- SWAGGER / OPENAPI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.6.0</version>
        </dependency>

        <!-- TEST -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

        </plugins>
    </build>

</project>


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\horario-service\src\main\java\com\reservacanchas\cl\horario_service\HorarioServiceApplication.java
====================================================

package com.reservacanchas.cl.horario_service;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class HorarioServiceApplication {

	public static void main(String[] args) {
		SpringApplication.run(HorarioServiceApplication.class, args);
	}

}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\horario-service\src\main\java\com\reservacanchas\cl\horario_service\config\SwaggerConfig.java
====================================================

package com.reservacanchas.cl.horario_service.config;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import io.swagger.v3.oas.models.servers.Server;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.List;

@Configuration
public class SwaggerConfig {

    @Bean
    public OpenAPI customOpenAPI() {

        Server gatewayServer = new Server();
        gatewayServer.setUrl("http://localhost:7090");
        gatewayServer.setDescription("API Gateway");

        return new OpenAPI()
                .servers(List.of(gatewayServer))
                .info(new Info()
                        .title("API Horario")
                        .version("1.0")
                        .description("DocumentaciÃ³n del microservicio Horario"));
    }
}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\horario-service\src\main\resources\application.yml
====================================================

server:
  port: 7096

spring:
  application:
    name: horario-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_horario_service?createDatabaseIfNotExist=true
    username: root
    password:

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\horario-service\target\classes\application.yml
====================================================

server:
  port: 7096

spring:
  application:
    name: horario-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_horario_service?createDatabaseIfNotExist=true
    username: root
    password:

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\mantenimiento-service\pom.xml
====================================================

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.12</version>
        <relativePath/>
    </parent>

    <groupId>com.reservacanchas.cl</groupId>
    <artifactId>mantenimiento-service</artifactId>
    <version>0.0.1-SNAPSHOT</version>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>

        <!-- WEB -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- JDBC -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>

        <!-- JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- LIQUIBASE -->
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>

        <!-- MYSQL -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- VALIDATION -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- LOMBOK -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- SWAGGER / OPENAPI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.6.0</version>
        </dependency>

        <!-- TEST -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

        </plugins>
    </build>

</project>


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\mantenimiento-service\src\main\java\com\reservacanchas\cl\mantenimiento_service\MantenimientoServiceApplication.java
====================================================

package com.reservacanchas.cl.mantenimiento_service;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class MantenimientoServiceApplication {

	public static void main(String[] args) {
		SpringApplication.run(MantenimientoServiceApplication.class, args);
	}

}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\mantenimiento-service\src\main\java\com\reservacanchas\cl\mantenimiento_service\config\SwaggerConfig.java
====================================================

package com.reservacanchas.cl.mantenimiento_service.config;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import io.swagger.v3.oas.models.servers.Server;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.List;

@Configuration
public class SwaggerConfig {

    @Bean
    public OpenAPI customOpenAPI() {

        Server gatewayServer = new Server();
        gatewayServer.setUrl("http://localhost:7090");
        gatewayServer.setDescription("API Gateway");

        return new OpenAPI()
                .servers(List.of(gatewayServer))
                .info(new Info()
                        .title("API Mantenimiento")
                        .version("1.0")
                        .description("DocumentaciÃ³n del microservicio Mantenimiento"));
    }
}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\mantenimiento-service\src\main\resources\application.yml
====================================================

server:
  port: 7100

spring:
  application:
    name: mantenimiento-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_mantenimiento_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\mantenimiento-service\target\classes\application.yml
====================================================

server:
  port: 7100

spring:
  application:
    name: mantenimiento-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_mantenimiento_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\notificacion-service\pom.xml
====================================================

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.12</version>
        <relativePath/>
    </parent>

    <groupId>com.reservacanchas.cl</groupId>
    <artifactId>notificacion-service</artifactId>
    <version>0.0.1-SNAPSHOT</version>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>

        <!-- WEB -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- JDBC -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>

        <!-- JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- LIQUIBASE -->
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>

        <!-- MYSQL -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- VALIDATION -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- LOMBOK -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- SWAGGER / OPENAPI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.6.0</version>
        </dependency>

        <!-- TEST -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

        </plugins>
    </build>

</project>


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\notificacion-service\src\main\java\com\reservacanchas\cl\notificacion_service\NotificacionServiceApplication.java
====================================================

package com.reservacanchas.cl.notificacion_service;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class NotificacionServiceApplication {

	public static void main(String[] args) {
		SpringApplication.run(NotificacionServiceApplication.class, args);
	}

}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\notificacion-service\src\main\java\com\reservacanchas\cl\notificacion_service\config\SwaggerConfig.java
====================================================

package com.reservacanchas.cl.notificacion_service.config;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import io.swagger.v3.oas.models.servers.Server;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.List;

@Configuration
public class SwaggerConfig {

    @Bean
    public OpenAPI customOpenAPI() {

        Server gatewayServer = new Server();
        gatewayServer.setUrl("http://localhost:7090");
        gatewayServer.setDescription("API Gateway");

        return new OpenAPI()
                .servers(List.of(gatewayServer))
                .info(new Info()
                        .title("API NotificaciÃ³n")
                        .version("1.0")
                        .description("DocumentaciÃ³n del microservicio NotificaciÃ³n"));
    }
}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\notificacion-service\src\main\resources\application.yml
====================================================

server:
  port: 7098

spring:
  application:
    name: notificacion-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_notificacion_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\notificacion-service\target\classes\application.yml
====================================================

server:
  port: 7098

spring:
  application:
    name: notificacion-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_notificacion_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\pago-service\pom.xml
====================================================

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.12</version>
        <relativePath/>
    </parent>

    <groupId>com.reservacanchas.cl</groupId>
    <artifactId>pago-service</artifactId>
    <version>0.0.1-SNAPSHOT</version>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>

        <!-- WEB -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- JDBC -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>

        <!-- JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- LIQUIBASE -->
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>

        <!-- MYSQL -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- VALIDATION -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- LOMBOK -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- SWAGGER / OPENAPI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.6.0</version>
        </dependency>

        <!-- TEST -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

        </plugins>
    </build>

</project>


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\pago-service\src\main\java\com\reservacanchas\cl\pago_service\PagoServiceApplication.java
====================================================

package com.reservacanchas.cl.pago_service;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class PagoServiceApplication {

	public static void main(String[] args) {
		SpringApplication.run(PagoServiceApplication.class, args);
	}

}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\pago-service\src\main\java\com\reservacanchas\cl\pago_service\config\SwaggerConfig.java
====================================================

package com.reservacanchas.cl.pago_service.config;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import io.swagger.v3.oas.models.servers.Server;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.List;

@Configuration
public class SwaggerConfig {

    @Bean
    public OpenAPI customOpenAPI() {

        Server gatewayServer = new Server();
        gatewayServer.setUrl("http://localhost:7090");
        gatewayServer.setDescription("API Gateway");

        return new OpenAPI()
                .servers(List.of(gatewayServer))
                .info(new Info()
                        .title("API Pago")
                        .version("1.0")
                        .description("DocumentaciÃ³n del microservicio Pago"));
    }
}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\pago-service\src\main\resources\application.yml
====================================================

server:
  port: 7094

spring:
  application:
    name: pago-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_pago_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\pago-service\target\classes\application.yml
====================================================

server:
  port: 7094

spring:
  application:
    name: pago-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_pago_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\resena-service\pom.xml
====================================================

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.12</version>
        <relativePath/>
    </parent>

    <groupId>com.reservacanchas.cl</groupId>
    <artifactId>resena-service</artifactId>
    <version>0.0.1-SNAPSHOT</version>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>

        <!-- WEB -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- JDBC -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>

        <!-- JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- LIQUIBASE -->
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>

        <!-- MYSQL -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- VALIDATION -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- LOMBOK -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- SWAGGER / OPENAPI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.6.0</version>
        </dependency>

        <!-- TEST -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

        </plugins>
    </build>

</project>


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\resena-service\src\main\java\com\reservacanchas\cl\resena_service\ResenaServiceApplication.java
====================================================

package com.reservacanchas.cl.resena_service;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class ResenaServiceApplication {

	public static void main(String[] args) {
		SpringApplication.run(ResenaServiceApplication.class, args);
	}

}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\resena-service\src\main\java\com\reservacanchas\cl\resena_service\config\SwaggerConfig.java
====================================================

package com.reservacanchas.cl.resena_service.config;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import io.swagger.v3.oas.models.servers.Server;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.List;

@Configuration
public class SwaggerConfig {

    @Bean
    public OpenAPI customOpenAPI() {

        Server gatewayServer = new Server();
        gatewayServer.setUrl("http://localhost:7090");
        gatewayServer.setDescription("API Gateway");

        return new OpenAPI()
                .servers(List.of(gatewayServer))
                .info(new Info()
                        .title("API ReseÃ±a")
                        .version("1.0")
                        .description("DocumentaciÃ³n del microservicio ReseÃ±a"));
    }
}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\resena-service\src\main\resources\application.yml
====================================================

server:
  port: 7099

spring:
  application:
    name: resena-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_resena_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\resena-service\target\classes\application.yml
====================================================

server:
  port: 7099

spring:
  application:
    name: resena-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_resena_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\reserva-service\pom.xml
====================================================

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.12</version>
        <relativePath/>
    </parent>

    <groupId>com.reservacanchas.cl</groupId>
    <artifactId>reserva-service</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>reserva-service</name>
    <description>Microservicio de reservas</description>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>

        <!-- API REST -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- JDBC -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>

        <!-- JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- LIQUIBASE -->
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>

        <!-- MYSQL -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- LOMBOK -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- VALIDATION -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- SECURITY -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- RESOURCE SERVER JWT -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>

        <!-- SWAGGER / OPENAPI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.6.0</version>
        </dependency>

        <!-- TEST -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

        </plugins>
    </build>

</project>


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\reserva-service\src\main\java\com\reservacanchas\cl\reserva_service\ReservaServiceApplication.java
====================================================

package com.reservacanchas.cl.reserva_service;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class ReservaServiceApplication {

	public static void main(String[] args) {
		SpringApplication.run(ReservaServiceApplication.class, args);
	}

}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\reserva-service\src\main\java\com\reservacanchas\cl\reserva_service\config\SecurityConfig.java
====================================================

package com.reservacanchas.cl.reserva_service.config;

import java.nio.charset.StandardCharsets;

import javax.crypto.spec.SecretKeySpec;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.web.SecurityFilterChain;

import org.springframework.security.oauth2.jose.jws.MacAlgorithm;
import org.springframework.security.oauth2.jwt.JwtDecoder;
import org.springframework.security.oauth2.jwt.NimbusJwtDecoder;

@Configuration
public class SecurityConfig {

    @Value("${spring.security.oauth2.resourceserver.jwt.secret-key}")
    private String secret;

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {

        http
                .csrf(csrf -> csrf.disable())
                .authorizeHttpRequests(auth -> auth

                        // SWAGGER / OPENAPI - pÃºblico
                        .requestMatchers(
                                "/v3/api-docs/**",
                                "/swagger-ui/**",
                                "/swagger-ui.html",
                                "/doc/swagger-ui.html",
                                "/doc/**"
                        ).permitAll()

                        // Endpoint pÃºblico que ya tenÃ­as
                        .requestMatchers("/reservas/*/exists").permitAll()

                        // Todo lo demÃ¡s requiere JWT
                        .anyRequest().authenticated()
                )
                .oauth2ResourceServer(oauth2 -> oauth2
                        .jwt(jwt -> {})
                );

        return http.build();
    }

    @Bean
    public JwtDecoder jwtDecoder() {

        SecretKeySpec key = new SecretKeySpec(
                secret.getBytes(StandardCharsets.UTF_8),
                "HmacSHA384"
        );

        return NimbusJwtDecoder
                .withSecretKey(key)
                .macAlgorithm(MacAlgorithm.HS384)
                .build();
    }
}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\reserva-service\src\main\java\com\reservacanchas\cl\reserva_service\config\SwaggerConfig.java
====================================================

package com.reservacanchas.cl.reserva_service.config;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import io.swagger.v3.oas.models.servers.Server;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.List;

@Configuration
public class SwaggerConfig {

    @Bean
    public OpenAPI customOpenAPI() {

        Server gatewayServer = new Server();
        gatewayServer.setUrl("http://localhost:7090");
        gatewayServer.setDescription("API Gateway");

        return new OpenAPI()
                .servers(List.of(gatewayServer))
                .info(new Info()
                        .title("API Reserva")
                        .version("1.0")
                        .description("DocumentaciÃ³n del microservicio Reserva"));
    }
}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\reserva-service\src\main\resources\application.yml
====================================================

server:
  port: 7093

spring:
  application:
    name: reserva-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_reserva_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  security:
    oauth2:
      resourceserver:
        jwt:
          secret-key: esta_es_nuestra_klae_secreta_123456789_viva_el_ping_pong

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\reserva-service\target\classes\application.yml
====================================================

server:
  port: 7093

spring:
  application:
    name: reserva-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_reserva_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  security:
    oauth2:
      resourceserver:
        jwt:
          secret-key: esta_es_nuestra_klae_secreta_123456789_viva_el_ping_pong

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\sede-service\pom.xml
====================================================

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.12</version>
        <relativePath/>
    </parent>

    <groupId>com.reservacanchas.cl</groupId>
    <artifactId>sede-service</artifactId>
    <version>0.0.1-SNAPSHOT</version>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>

        <!-- WEB -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- JDBC -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>

        <!-- JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- MYSQL -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- VALIDATION -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- FLYWAY -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>

        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-mysql</artifactId>
        </dependency>

        <!-- LOMBOK -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- SWAGGER / OPENAPI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.6.0</version>
        </dependency>

        <!-- TEST -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

        </plugins>
    </build>

</project>


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\sede-service\src\main\java\com\reservacanchas\cl\sede_service\SedeServiceApplication.java
====================================================

package com.reservacanchas.cl.sede_service;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class SedeServiceApplication {

	public static void main(String[] args) {
		SpringApplication.run(SedeServiceApplication.class, args);
	}

}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\sede-service\src\main\java\com\reservacanchas\cl\sede_service\config\SwaggerConfig.java
====================================================

package com.reservacanchas.cl.sede_service.config;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import io.swagger.v3.oas.models.servers.Server;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.List;

@Configuration
public class SwaggerConfig {

    @Bean
    public OpenAPI customOpenAPI() {

        Server gatewayServer = new Server();
        gatewayServer.setUrl("http://localhost:7090");
        gatewayServer.setDescription("API Gateway");

        return new OpenAPI()
                .servers(List.of(gatewayServer))
                .info(new Info()
                        .title("API Sede")
                        .version("1.0")
                        .description("DocumentaciÃ³n del microservicio Sede"));
    }
}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\sede-service\src\main\resources\application.yml
====================================================

server:
  port: 7095

spring:
  application:
    name: sede-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_sede_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

  liquibase:
    enabled: false

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\sede-service\target\classes\application.yml
====================================================

server:
  port: 7095

spring:
  application:
    name: sede-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_sede_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

  liquibase:
    enabled: false

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\usuario-service\pom.xml
====================================================

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.12</version>
        <relativePath/>
    </parent>

    <groupId>com.reservacanchas.cl</groupId>
    <artifactId>usuario-service</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>usuario-service</name>
    <description>Microservicio de usuarios</description>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>

        <!-- WEB REST -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- JDBC -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>

        <!-- JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- LIQUIBASE -->
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>

        <!-- MYSQL -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- LOMBOK -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- VALIDATION -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- SECURITY -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- RESOURCE SERVER JWT -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>

        <!-- SWAGGER / OPENAPI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.6.0</version>
        </dependency>

        <!-- TEST -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

        </plugins>
    </build>

</project>


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\usuario-service\src\main\java\com\reservacanchas\cl\usuario_service\UsuarioServiceApplication.java
====================================================

package com.reservacanchas.cl.usuario_service;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class UsuarioServiceApplication {

	public static void main(String[] args) {
		SpringApplication.run(UsuarioServiceApplication.class, args);
	}

}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\usuario-service\src\main\java\com\reservacanchas\cl\usuario_service\config\SecurityConfig.java
====================================================

package com.reservacanchas.cl.usuario_service.config;

import java.nio.charset.StandardCharsets;

import javax.crypto.spec.SecretKeySpec;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.web.SecurityFilterChain;

import org.springframework.security.oauth2.jose.jws.MacAlgorithm;
import org.springframework.security.oauth2.jwt.JwtDecoder;
import org.springframework.security.oauth2.jwt.NimbusJwtDecoder;

@Configuration
public class SecurityConfig {

    @Value("${spring.security.oauth2.resourceserver.jwt.secret-key}")
    private String secret;

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {

        http
                .csrf(csrf -> csrf.disable())
                .authorizeHttpRequests(auth -> auth

                        // SWAGGER / OPENAPI - pÃºblico
                        .requestMatchers(
                                "/v3/api-docs/**",
                                "/swagger-ui/**",
                                "/swagger-ui.html",
                                "/doc/swagger-ui.html",
                                "/doc/**"
                        ).permitAll()

                        // Endpoint pÃºblico que ya tenÃ­as
                        .requestMatchers("/usuarios/*/exists").permitAll()

                        // Todo lo demÃ¡s requiere JWT
                        .anyRequest().authenticated()
                )
                .oauth2ResourceServer(oauth2 -> oauth2
                        .jwt(jwt -> {})
                );

        return http.build();
    }

    @Bean
    public JwtDecoder jwtDecoder() {

        SecretKeySpec key = new SecretKeySpec(
                secret.getBytes(StandardCharsets.UTF_8),
                "HmacSHA384"
        );

        return NimbusJwtDecoder
                .withSecretKey(key)
                .macAlgorithm(MacAlgorithm.HS384)
                .build();
    }
}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\usuario-service\src\main\java\com\reservacanchas\cl\usuario_service\config\SwaggerConfig.java
====================================================

package com.reservacanchas.cl.usuario_service.config;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import io.swagger.v3.oas.models.servers.Server;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.List;

@Configuration
public class SwaggerConfig {

    @Bean
    public OpenAPI customOpenAPI() {

        Server gatewayServer = new Server();
        gatewayServer.setUrl("http://localhost:7090");
        gatewayServer.setDescription("API Gateway");

        return new OpenAPI()
                .servers(List.of(gatewayServer))
                .info(new Info()
                        .title("API Usuario")
                        .version("1.0")
                        .description("DocumentaciÃ³n del microservicio Usuario"));
    }
}


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\usuario-service\src\main\resources\application.yml
====================================================

server:
  port: 7091

spring:
  application:
    name: usuario-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_usuario_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  security:
    oauth2:
      resourceserver:
        jwt:
          secret-key: esta_es_nuestra_klae_secreta_123456789_viva_el_ping_pong

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html


====================================================
ARCHIVO: C:\Users\diber\Downloads\reserva-canchas\usuario-service\target\classes\application.yml
====================================================

server:
  port: 7091

spring:
  application:
    name: usuario-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_usuario_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  security:
    oauth2:
      resourceserver:
        jwt:
          secret-key: esta_es_nuestra_klae_secreta_123456789_viva_el_ping_pong

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html
